<IfModule mod_rewrite.c>
  RewriteEngine On

  # Redirect root request to htdocs/dist/ if available, else htdocs/
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^$ htdocs/dist/index.html [L]
</IfModule>
